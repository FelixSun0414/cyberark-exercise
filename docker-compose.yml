services:
  backend:
    build: ./backend
    working_dir: /app
    #working_dir: /backend
    #volumes:
    #  - "${PWD}/backend:/backend"
    ports:
      - "5000:5000"
    
  frontend:
    build: ./frontend
    working_dir: /app
    #working_dir: /frontend
    #volumes:
    #  - "${PWD}/frontend:/frontend"
    ports:
      - "3000:3000"
    depends_on:
      - backend
